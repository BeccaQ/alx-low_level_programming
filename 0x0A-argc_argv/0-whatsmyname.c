#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * main - prints name of the program
 * Return: 0 on success
 */

int main(__attribute__ ((unused)) int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
