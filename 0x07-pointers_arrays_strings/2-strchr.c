#include <stdio.h>
#include "main.h"

/**
 * _strchr -  that locates a character in a string
 * @s: pointer to a char
 * @c: a char
 * Return: a pointer
 */

char *_strchr(char *s, char c)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
		if *(s + i) == 'c'
		{
			break;
		}

		return (*s);
}
