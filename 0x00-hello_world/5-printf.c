#include <stdio.h>

/**
 * main - prints the string with proper grammar, but the outcome is a piece of art,
 * return: 0 if properly executed otherwise non-zero
 */

int main(void)
{
	printf ("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
